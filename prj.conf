CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3

# GPIO Configuration
CONFIG_GPIO=y

# PWM Configuration
CONFIG_PWM=y

# ESP32-S3 Specific - LEDC (LED Controller for PWM)
CONFIG_PWM_LED_ESP32=y

# Timing
CONFIG_HELLO_TIMER_INTERVAL_MS=1000

# Serial/Console (para debug)
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
